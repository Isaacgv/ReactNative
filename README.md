# Install Expo Global
npm install -g expo-cli

# Init Project
expo init imhere --npm

# Start Project
npx expo start

expo init imhere --npm
expo start
npx expo start
npx expo install

npx expo init imhere --npm